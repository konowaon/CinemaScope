<h1>Reviews</h1>
<% @reviews.each do |review| %>
  <div>
    <h2><%= link_to review.movie.title, review_path(review) %></h2>
    <p><%= review.content %></p>
    <p>Rating: <%= review.rating %></p>
  </div>
<% end %>